import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Zyra Edutech - Course Registration Portal",
  description: "Register for premium courses at Zyra Edutech. Expert-led learning with lifetime support.",
  keywords: "education, online courses, learning, Zyra Edutech",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
